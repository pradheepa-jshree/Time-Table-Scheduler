def format_violation(msg: str) -> str:
    """
    Format a violation message with emoji prefix.
    
    Args:
        msg: The violation message
    
    Returns:
        Formatted violation string with emoji prefix
    """
    return f"⚠ {msg}"
